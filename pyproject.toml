[project]
name = "ja-CLI"
version = "0.73"
description = "Minimal journaling CLI for raw thought capture"
authors = [{name = "Paul"}]
readme = "README.md"
requires-python = ">=3.10"

dependencies = []

[project.scripts]
_ja = "ja.main:main"

[tool.setuptools]
packages = ["ja"]